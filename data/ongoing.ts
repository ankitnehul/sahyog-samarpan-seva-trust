import type { OngoingEvent } from "@/types";

/**
 * Currently active / featured event shown prominently on the homepage.
 * Set to `null` when there is no ongoing event.
 */
export const ongoingEvent: OngoingEvent | null = null;
